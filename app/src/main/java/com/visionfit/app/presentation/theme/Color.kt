package com.visionfit.app.presentation.theme

import androidx.compose.ui.graphics.Color

/**
 * Color - Definição de cores do tema VisionFit
 *
 * Cores da paleta:
 * - Azul Escuro: principal
 * - Azul Claro: secundária
 * - Branco: fundo
 *
 * @author VisionFit Team
 */

// Cores principais
val DarkBlue = Color(0xFF1E3A8A)      // Azul Escuro - #1E3A8A
val LightBlue = Color(0xFF3B82F6)     // Azul Claro - #3B82F6
val White = Color(0xFFFFFFFF)         // Branco - #FFFFFF

// Cores adicionais para acessibilidade
val LightGray = Color(0xFFF3F4F6)     // Cinza claro para backgrounds
val DarkGray = Color(0xFF374151)      // Cinza escuro para textos
val Success = Color(0xFF10B981)       // Verde para sucesso
val Warning = Color(0xFFF59E0B)       // Laranja para aviso
val Error = Color(0xFFEF4444)         // Vermelho para erro
val Info = Color(0xFF06B6D4)          // Azul claro para informação

// Cores de vibração (feedback háptico)
val VibrationGreen = Color(0xFF22C55E)    // Verde vivo para confirmação
val VibrationOrange = Color(0xFFFB923C)   // Laranja para alerta
val VibrationRed = Color(0xFFF87171)      // Vermelho para perigo